services:
  # SCUFFBOT
  bot:
    build: ./
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml.local
      - .local-secrets:/app/.local-secrets
    env_file:
      - ./.env.local
